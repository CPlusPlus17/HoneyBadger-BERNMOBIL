import React from "react";
import { Map, TileLayer, Marker, Popup } from "react-leaflet";
import MarkerComponent from "../MarkerComponent";

const App = ({ position, zoom, icon }) => (
    <div>
        <h2>Hello Bernmobil</h2>
    </div>
);

export default App;
